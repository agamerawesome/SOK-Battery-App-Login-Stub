# SOK-Battery-App-Login-Stub
 A Node.js stub server that bypasses cloud login on the SOK Battery BMS app (`com.sok.batterypacks`). Allows you to use the app offline without sending credentials to Tencent's servers.
